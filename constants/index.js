const contractAddresses = require("./contractAddresses.json")
const abi = require("./abi.json")
const moralisServer = require("./moralisServer.json")

module.exports = {
    contractAddresses,
    abi,
    moralisServer,
}
